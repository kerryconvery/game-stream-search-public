import AddChannelForm from '../AddChannelForm';
import { useGameStreamApi } from '../../../../../api/gameStreamApi';

jest.mock('../../../../../api/gameStreamApi');

describe('Add Channel Form', () => {
  it('should ', () => {
    expect(true).toEqual(true);
  })
})